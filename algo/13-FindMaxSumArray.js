/*
Question: Verilen bir dizi içindeki sayıları, onları ardışık olarak toplayarak en büyük toplamı
elde eden alt diziyi bulun. Bu işlemi gerçekleştiren bir JavaScript fonksiyonu yazın.


Girdi: [-2, 1, -3, 4, -1, 2, 1, -5, 4]
Çıktı: [4, -1, 2, 1]
 */

const findMaxSum= (arr)=>{

    for (let i = 0; i <arr.length; i++) {

    }
}
const arr=[-2, 1, -3, 4, -1, 2, 1, -5, 4]
console.log(findMaxSum(arr))